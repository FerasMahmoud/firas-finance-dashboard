# 💰 لوحة التحكم المالية

لوحة تحكم مالية شخصية باللغة العربية مع دعم RTL، وضع داكن/فاتح، ورسوم بيانية تفاعلية.

## ✨ المميزات

### 📊 العرض الرئيسي
- إجمالي الرصيد (جميع البنوك)
- رصيد كل بنك على حدة
- الدخل مقابل المصروفات (الشهر الحالي)
- آخر 10 معاملات
- رسوم بيانية للمصروفات

### 🔍 التصفية
- حسب البنك (السعودي الفرنسي، الراجحي، برق، تيكمو، STC Bank)
- حسب الفئة (طعام، تسوق، تبرعات، خدمات تقنية، تحويلات)
- حسب التصنيف (شخصي، عمل، عائلة)
- حسب التاريخ (يوم، أسبوع، شهر)

### 📈 التقارير
- ملخص يومي
- ملخص أسبوعي
- ملخص شهري
- مقارنة شهرية

### 🎨 التصميم
- وضع داكن (افتراضي) + وضع فاتح
- دعم RTL (عرض من اليمين)
- تصميم نظيف وبسيط
- متجاوب (صديق للموبايل)

## 🛠️ التقنيات المستخدمة

- **Vanilla JavaScript** - بدون إطار عمل، سريع وخفيف
- **Tailwind CSS** - للتصميم المتجاوب والوضع الداكن
- **Chart.js** - للرسوم البيانية التفاعلية
- **Static JSON** - لا يحتاج قاعدة بيانات أو خادم

## 📁 هيكل الملفات

```
finance-dashboard/
├── index.html           # الصفحة الرئيسية
├── app.js              # منطق التطبيق
├── data/
│   ├── transactions.json   # بيانات المعاملات
│   └── balances.json       # أرصدة البنوك
├── _headers            # إعدادات Cloudflare Pages
└── README.md           # هذا الملف
```

## 📝 هيكل البيانات

### transactions.json
```json
[
  {
    "id": 1,
    "timestamp": "2026-02-02T10:30:00Z",
    "bank": "banque-saudi",
    "amount": -150.50,
    "merchant": "مطعم النخيل",
    "category": "طعام",
    "classification": "شخصي",
    "note": "غداء مع الأصدقاء",
    "confirmed": true
  }
]
```

**الحقول:**
- `id` - معرف فريد للمعاملة
- `timestamp` - التاريخ والوقت (ISO 8601)
- `bank` - البنك (`banque-saudi`, `alrajhi`, `barq`, `tikmo`, `stc`)
- `amount` - المبلغ (موجب للدخل، سالب للمصروفات)
- `merchant` - التاجر/المصدر
- `category` - الفئة (`طعام`, `تسوق`, `تبرعات`, `خدمات تقنية`, `تحويلات`, `دخل`)
- `classification` - التصنيف (`شخصي`, `عمل`, `عائلة`)
- `note` - ملاحظة (اختياري)
- `confirmed` - مؤكد أم لا

### balances.json
```json
{
  "banque-saudi": 15000.00,
  "alrajhi": 8500.00,
  "barq": 2000.00,
  "tikmo": 1500.00,
  "stc": 3000.00
}
```

## 🚀 النشر على Cloudflare Pages

### الطريقة 1: من خلال Dashboard

1. افتح [Cloudflare Pages](https://pages.cloudflare.com/)
2. اضغط "Create a project"
3. اختر "Upload assets"
4. ارفع المجلد `finance-dashboard` بالكامل
5. تم! الموقع سيكون متاح على `https://your-project.pages.dev`

### الطريقة 2: من خلال Git

1. أنشئ مستودع جديد على GitHub
2. ارفع ملفات المشروع:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin YOUR_REPO_URL
   git push -u origin main
   ```
3. في Cloudflare Pages، اختر "Connect to Git"
4. اختر المستودع
5. إعدادات البناء:
   - **Build command:** (اتركه فارغًا)
   - **Build output directory:** `/`
6. اضغط "Save and Deploy"

### الطريقة 3: باستخدام Wrangler CLI

```bash
# تثبيت Wrangler
npm install -g wrangler

# تسجيل الدخول
wrangler login

# النشر
cd finance-dashboard
wrangler pages deploy . --project-name=finance-dashboard
```

## 📱 الاستخدام المحلي

1. افتح المجلد:
   ```bash
   cd finance-dashboard
   ```

2. شغّل خادم محلي (أي طريقة):
   ```bash
   # Python 3
   python3 -m http.server 8000
   
   # Node.js (http-server)
   npx http-server
   
   # PHP
   php -S localhost:8000
   ```

3. افتح المتصفح على `http://localhost:8000`

## 🔄 تحديث البيانات

### إضافة معاملات جديدة

عدّل ملف `data/transactions.json` وأضف معاملات جديدة بنفس التنسيق:

```json
{
  "id": 13,
  "timestamp": "2026-02-02T15:00:00Z",
  "bank": "stc",
  "amount": -75.00,
  "merchant": "مقهى",
  "category": "طعام",
  "classification": "شخصي",
  "note": "",
  "confirmed": true
}
```

### تحديث الأرصدة

عدّل ملف `data/balances.json`:

```json
{
  "banque-saudi": 16000.00,
  "alrajhi": 9000.00,
  "barq": 2500.00,
  "tikmo": 1800.00,
  "stc": 3500.00
}
```

## 🎯 نصائح الاستخدام

### أتمتة تحديث البيانات

يمكنك إنشاء سكربت لتحديث البيانات تلقائيًا:

```javascript
// update-data.js (Node.js)
const fs = require('fs');

// قراءة البيانات الحالية
const transactions = JSON.parse(fs.readFileSync('data/transactions.json'));

// إضافة معاملة جديدة
transactions.push({
  id: transactions.length + 1,
  timestamp: new Date().toISOString(),
  bank: 'stc',
  amount: -100,
  merchant: 'مطعم',
  category: 'طعام',
  classification: 'شخصي',
  note: '',
  confirmed: true
});

// حفظ
fs.writeFileSync('data/transactions.json', JSON.stringify(transactions, null, 2));
```

### النسخ الاحتياطي

احفظ نسخة من ملفات البيانات بانتظام:

```bash
# نسخة يومية
cp data/transactions.json backups/transactions-$(date +%Y%m%d).json
cp data/balances.json backups/balances-$(date +%Y%m%d).json
```

## 🔒 الأمان

- البيانات محفوظة محليًا فقط (لا تُرسل لأي خادم)
- يُنصح بحماية الموقع بكلمة مرور إذا كان عامًا
- استخدم Cloudflare Access للحماية الإضافية

### حماية الموقع بكلمة مرور (Cloudflare Access)

1. في لوحة Cloudflare، اذهب إلى "Zero Trust"
2. اختر "Access" > "Applications"
3. أنشئ تطبيق جديد واختر موقع Pages
4. أضف سياسات الدخول (بريد إلكتروني، PIN، إلخ)

## 🐛 استكشاف الأخطاء

### البيانات لا تظهر
- تأكد من وجود ملفات `data/transactions.json` و `data/balances.json`
- تحقق من صحة تنسيق JSON (استخدم [JSONLint](https://jsonlint.com/))
- افتح Console في المتصفح (F12) للتحقق من الأخطاء

### الرسوم البيانية لا تعمل
- تأكد من اتصال الإنترنت (Chart.js يُحمل من CDN)
- جرب مسح الكاش (Ctrl + Shift + R)

### الوضع الداكن لا يحفظ
- تأكد من تفعيل localStorage في المتصفح
- جرب في وضع التصفح العادي (ليس الخاص)

## 📞 الدعم

لأي استفسارات أو مشاكل، يمكنك:
- فتح Issue في المستودع
- التواصل مع المطور

## 📄 الترخيص

هذا المشروع للاستخدام الشخصي. يمكنك تعديله واستخدامه كما تشاء.

---

**صُنع بـ ❤️ لفراس**