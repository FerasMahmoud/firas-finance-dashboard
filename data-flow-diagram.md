# 📊 Data Flow Diagram - Finance Dashboard

## Complete Data Journey (With Issues Highlighted)

```
┌───────────────────────────────────────────────────────────────┐
│                    🌐 USER OPENS PAGE                         │
└──────────────────────┬────────────────────────────────────────┘
                       │
                       ▼
┌───────────────────────────────────────────────────────────────┐
│              DOMContentLoaded Event Fires                     │
└──────────────────────┬────────────────────────────────────────┘
                       │
                       ▼
┌───────────────────────────────────────────────────────────────┐
│                    await loadData()                           │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  Promise.all([                                         │  │
│  │    fetch('data/transactions.json'),  ← HTTP GET        │  │
│  │    fetch('data/balances.json')       ← HTTP GET        │  │
│  │  ])                                                     │  │
│  └────────────┬──────────────┬────────────────────────────┘  │
│               │              │                                │
│      ┌────────▼────────┐     ▼──────────────┐                │
│      │  🚨 NO STATUS   │  Network Error?    │                │
│      │     CHECK!      │  CORS Error?       │                │
│      │                 │  Timeout?          │                │
│      │  ❌ 404 HTML → │  Malformed JSON?   │                │
│      │  tries to parse │                    │                │
│      │  as JSON!       │                    │                │
│      └────────┬────────┘     ┌──────────────┘                │
│               │              │                                │
│               │      ┌───────▼─────────┐                      │
│               │      │   catch block   │                      │
│               │      │                 │                      │
│               ▼      │  getSample...() │                      │
│         await res.json()  └───────┬─────┘                     │
│               │                   │                           │
│      ┌────────▼─────────┐         │                           │
│      │  🚨 NO DATA      │         │                           │
│      │  VALIDATION!     │         │                           │
│      │                  │         │                           │
│      │ ❌ Missing banks │         │                           │
│      │ ❌ Invalid types │         │                           │
│      │ ❌ Bad structure │         │                           │
│      └────────┬─────────┘         │                           │
│               │                   │                           │
│               ├───────────────────┤                           │
│               │                   │                           │
└───────────────┼───────────────────┼───────────────────────────┘
                │                   │
                ▼                   ▼
┌───────────────────────────────────────────────────────────────┐
│                  📦 STORE IN GLOBALS                          │
│                                                               │
│  transactions = [...] ← Raw data (267 items)                 │
│  balances = {...}     ← 5 banks                              │
│  filteredTransactions = [...transactions] ← Clone            │
│                                                               │
│  🚨 NO TRANSFORMATION applied yet!                           │
│  🚨 NO NORMALIZATION!                                        │
│  🚨 Still contains unmapped banks, conflicts, nulls          │
└──────────────────────┬────────────────────────────────────────┘
                       │
                       ▼
┌───────────────────────────────────────────────────────────────┐
│                 initThemeToggle()                             │
│                 initFilters() ← Add event listeners           │
│                 renderDashboard() ← First render              │
└──────────────────────┬────────────────────────────────────────┘
                       │
         ┌─────────────┼─────────────┐
         │             │             │
         ▼             ▼             ▼
   renderBalances  renderIncome  renderTrans
                   Expenses       List
         │             │             │
         └─────────────┴─────────────┘
                       │
                       ▼
┌───────────────────────────────────────────────────────────────┐
│           🔄 TRANSFORMATION HAPPENS HERE                      │
│              (Repeated on EVERY render!)                      │
│                                                               │
│  transactions.forEach(t => {                                 │
│      // 🚨 Calculated every time (no caching)                │
│      const isExpense = t.transactionType === 'صرف' ||        │
│                        (t.amount < 0 && ...)                  │
│                         ^^^ BROKEN LOGIC!                     │
│                         Data has NO negative amounts!         │
│                                                               │
│      const bankName = bankNames[t.bank] || t.bank             │
│                       ^^^ 2 banks NOT mapped!                 │
│                                                               │
│      const amount = typeof data === 'number' ? ...            │
│  })                                                           │
└───────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════

## DATA TRANSFORMATION BREAKDOWN

### Current State (Scattered Logic):

```
┌─────────────────────────────────────────────────────┐
│  renderIncomeExpenses()                             │
│  ┌───────────────────────────────────────────────┐ │
│  │ monthlyTransactions.forEach(t => {            │ │
│  │   const isExpense = t.transactionType === ... │ │  ← Logic 1
│  │   if (isExpense) expenses += ...              │ │
│  │ })                                             │ │
│  └───────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│  renderTransactionsList()                           │
│  ┌───────────────────────────────────────────────┐ │
│  │ last10.map(t => {                             │ │
│  │   const isIncome = t.transactionType === ... │ │  ← Logic 2 (duplicate!)
│  │   const colorClass = isIncome ? ...          │ │
│  │   return `<div>...</div>`                     │ │
│  │ })                                             │ │
│  └───────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│  renderCategoryChart()                              │
│  ┌───────────────────────────────────────────────┐ │
│  │ filteredTransactions.forEach(t => {           │ │
│  │   const isExpense = t.transactionType === ... │ │  ← Logic 3 (duplicate!)
│  │   if (isExpense) categoryData[t.category]++  │ │
│  │ })                                             │ │
│  └───────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│  showReport()                                       │
│  ┌───────────────────────────────────────────────┐ │
│  │ transactions.forEach(t => {                   │ │
│  │   const isIncome = t.transactionType === ... │ │  ← Logic 4 (duplicate!)
│  │   if (isIncome) income += ...                 │ │
│  │ })                                             │ │
│  └───────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘

⚠️ SAME LOGIC REPEATED 5+ TIMES!
⚠️ PERFORMANCE: Recalculated on every render
⚠️ MAINTENANCE: Fix bug → must update 5 places
```

### Recommended Approach (Transform Once):

```
┌───────────────────────────────────────────────────────────────┐
│                      loadData()                               │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  1. Fetch JSON                                         │  │
│  │  2. Validate structure ✅                             │  │
│  │  3. Normalize data:                                    │  │
│  │     transactions = raw.map(normalizeTransaction)       │  │
│  │                                                         │  │
│  │  function normalizeTransaction(t) {                    │  │
│  │    return {                                            │  │
│  │      ...t,                                             │  │
│  │      // Pre-calculate everything once:                 │  │
│  │      bankName: bankNames[t.bank] || t.bank,           │  │
│  │      isIncome: t.transactionType === 'دخل',          │  │
│  │      isExpense: t.transactionType === 'صرف',         │  │
│  │      isTransfer: t.transactionType === 'تحويل',      │  │
│  │      absAmount: Math.abs(t.amount),                   │  │
│  │      date: new Date(t.timestamp),                     │  │
│  │      merchant: t.merchant || 'غير محدد',             │  │
│  │      category: t.category || 'غير محدد',             │  │
│  │      displayAmount: formatCurrency(Math.abs(t.amount))│  │
│  │    };                                                  │  │
│  │  }                                                     │  │
│  └────────────────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────────────┘
                       │
                       ▼
┌───────────────────────────────────────────────────────────────┐
│              ✅ NORMALIZED DATA IN MEMORY                     │
│                                                               │
│  transactions = [                                            │
│    {                                                         │
│      id: 1,                                                  │
│      timestamp: "2026-02-01...",                            │
│      bank: "السعودي الفرنسي",                               │
│      bankName: "السعودي الفرنسي", ← Pre-mapped              │
│      amount: 33.66,                                          │
│      absAmount: 33.66,             ← Pre-calculated          │
│      isIncome: false,              ← Pre-determined          │
│      isExpense: true,              ← Pre-determined          │
│      isTransfer: false,            ← Pre-determined          │
│      date: Date object,            ← Pre-parsed              │
│      merchant: "Keeta Mada",                                │
│      category: "طعام - مطاعم",                              │
│      displayAmount: "33.66 ر.س"   ← Pre-formatted          │
│    },                                                        │
│    ...                                                       │
│  ]                                                           │
└───────────────────────────────────────────────────────────────┘
                       │
                       ▼
┌───────────────────────────────────────────────────────────────┐
│              🚀 RENDERING (Now Fast!)                         │
│                                                               │
│  renderIncomeExpenses() {                                    │
│    transactions.forEach(t => {                               │
│      if (t.isExpense) expenses += t.absAmount; ← Simple!     │
│      if (t.isIncome) income += t.absAmount;                  │
│    });                                                       │
│  }                                                           │
│                                                               │
│  renderTransactionsList() {                                  │
│    last10.map(t => `                                         │
│      <div>                                                   │
│        <span>${t.bankName}</span>  ← Already mapped          │
│        <span>${t.displayAmount}</span> ← Already formatted   │
│      </div>                                                  │
│    `);                                                       │
│  }                                                           │
│                                                               │
│  ✅ NO REPEATED CALCULATIONS                                │
│  ✅ NO DUPLICATE LOGIC                                      │
│  ✅ CONSISTENT EVERYWHERE                                   │
└───────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════

## FILTER FLOW

┌───────────────────────────────────────────────────────────────┐
│              User Changes Filter Dropdown                     │
└──────────────────────┬────────────────────────────────────────┘
                       │
                       ▼
┌───────────────────────────────────────────────────────────────┐
│                  applyFilters()                               │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  filteredTransactions = transactions.filter(t => {     │  │
│  │    if (bank && t.bank !== bank) return false;          │  │
│  │    if (category && t.category !== category) ...        │  │
│  │    if (period === 'today') {                           │  │
│  │      // 🚨 NO TIMEZONE HANDLING                        │  │
│  │      if (date.toDateString() !== now.toDateString())   │  │
│  │    }                                                    │  │
│  │    return true;                                        │  │
│  │  });                                                   │  │
│  └────────────────────────────────────────────────────────┘  │
└──────────────────────┬────────────────────────────────────────┘
                       │
                       ▼
┌───────────────────────────────────────────────────────────────┐
│               renderDashboard()                               │
│            (Re-renders everything)                            │
└───────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════

## BALANCE RENDERING FLOW

┌───────────────────────────────────────────────────────────────┐
│                  renderBalances()                             │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  Object.entries(balances).forEach(([bank, data]) => { │  │
│  │                                                         │  │
│  │    // Handle both formats:                             │  │
│  │    const amount = typeof data === 'number' ?           │  │
│  │                   data : (data.balance || 0);          │  │
│  │                                                         │  │
│  │    total += amount;                                    │  │
│  │                                                         │  │
│  │    // Map bank name → element ID                       │  │
│  │    const bankId = bankIdMap[bank];                     │  │
│  │    // 🚨 2 banks NOT in bankIdMap!                     │  │
│  │                                                         │  │
│  │    if (bankId) {                                       │  │
│  │      const el = document.getElementById(`balance-${bankId}`);│
│  │      // 🚨 If element doesn't exist → silent fail     │  │
│  │      if (el) {                                         │  │
│  │        el.textContent = formatCurrency(amount);        │  │
│  │      }                                                 │  │
│  │    }                                                   │  │
│  │  });                                                   │  │
│  │                                                         │  │
│  │  document.getElementById('totalBalance')               │  │
│  │    .textContent = formatCurrency(total);               │  │
│  └────────────────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════

## ERROR SCENARIOS

### Scenario 1: File Not Found (404)
```
fetch('data/transactions.json')
  → HTTP 404
  → Returns HTML error page
  → await response.json() ← Tries to parse HTML as JSON
  → SyntaxError: Unexpected token '<'
  → catch block
  → getSampleTransactions()
  → 🚨 User sees fake data, doesn't know!
```

### Scenario 2: Malformed JSON
```
fetch('data/transactions.json')
  → HTTP 200 OK
  → Content: [{...},] ← Trailing comma
  → await response.json() ← Parse error
  → SyntaxError: Unexpected token
  → catch block
  → getSampleTransactions()
  → 🚨 User sees fake data, doesn't know!
```

### Scenario 3: Empty File
```
fetch('data/transactions.json')
  → HTTP 200 OK
  → Content: []
  → transactions = []
  → filteredTransactions = []
  → renderDashboard()
  → 🚨 Dashboard shows "no transactions" but no error
```

### Scenario 4: Unmapped Bank
```
transaction.bank = "برق"
bankNames["برق"] = undefined
const bankName = bankNames[t.bank] || t.bank
  → bankName = "برق" (fallback to original)
bankIdMap["برق"] = undefined
const bankId = bankIdMap[bank]
  → bankId = undefined
if (bankId) { ... } ← Skipped
  → 🚨 Balance not displayed, no warning
```

### Scenario 5: Transaction Type Conflict
```
transaction = {
  transactionType: "صرف",
  amount: 33.66  ← Positive!
}

const isExpense = t.transactionType === 'صرف' || 
                  (t.amount < 0 && !t.transactionType)
  → isExpense = true (✅ Correct - type takes precedence)

const isIncome = t.transactionType === 'دخل' || 
                 (t.amount > 0 && t.transactionType !== 'صرف' && ...)
  → isIncome = false (✅ Correct - 'صرف' excluded)

BUT: If transactionType is missing:
  → isExpense = false (amount > 0)
  → isIncome = true (amount > 0)
  → 🚨 WRONG! Should be expense based on context
```

═══════════════════════════════════════════════════════════════
